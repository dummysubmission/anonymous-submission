defaults:
  - model: qwen3-vl-2b-instruct
  - vllm_offline_chat
  - _self_

# Required at run time: benchmark stem for ``data/samples/<benchmark>.json``.
benchmark: null

save_path: null

start_index: 0
max_samples: null
overwrite: false
verbose: true
enable_thinking: false

temperature: 0.7

sampling_params:
  temperature: 0.7
  top_p: 1.0
  max_new_tokens: 65536
